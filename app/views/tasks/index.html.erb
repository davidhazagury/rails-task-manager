<h1>My tasks</h1>
<!-- Display the tasks list, @tasks is an array from the controller.-->
<ul>
  <% @tasks.each do|task| %>
    <li style="list-style: none">
      <%= task.completed ? "✅" : "❌" %> <%= link_to task.title, task_path(task.id) %>
    </li>
  <% end %>
</ul>
